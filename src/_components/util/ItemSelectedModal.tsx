import React, { useState } from 'react';
import { View } from 'react-native';


const ItemSelectedModal: React.FC = () => {
    const [options] = useState(() => {

            return 'a';
    })
    

    return <View>
        </View>
}

export default ItemSelectedModal;